<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title><%= pageTitle %></title>
    <link rel="stylesheet" href="/css/main.css" />
    <link rel="icon" type="image/x-icon" href="/images/favicon.ico" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css"
    />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css?family=Roboto:100,300,400,500,700"
    />
  </head>

  <body>
    <header>
      <div>
        <a href="/"><h1>Edit book</h1> </a>
      </div>
      <div style="margin-right: 30px">
        <a href="/" class="card">Home</a>
        <a href="/authors" class="card">Authors</a>
        <a href="/users" class="card">Users</a>
      </div>
    </header>
    <main>
      <form action="/edit-book" method="POST" class="add-form">
        <h2 class="form-heading">Edit Book</h2>

        <div class="form-field">
          <label for="title">Title:</label>
          <input
            type="text"
            name="title"
            id="title"
            value="<%= book.title %>"
            class="form-input"
            disabled
          />
        </div>

        <div class="form-field">
          <label for="author">Author:</label>
          <input
            type="text"
            name="author"
            id="auth"
            value="<%= book.username %>"
            disabled
            class="form-input"
          />
        </div>

        <div class="form-field">
          <label for="description">Description:</label>
          <input
            type="text"
            name="description"
            id="description"
            value="<%= book.description %>"
            class="form-input"
            disabled
          />
        </div>

        <div class="form-field">
          <label for="genre">Genre:</label>
          <input
            type="text"
            name="genre"
            id="genre"
            value="<%= book.name %>"
            class="form-input"
            disabled
          />
        </div>

        <div class="form-field">
          <label for="cover_image_url">Image url:</label>
          <input
            type="text"
            name="cover_image_url"
            id="cover_image_url"
            value="<%= book.cover_image_url %>"
            class="form-input"
          />
        </div>

        <div class="form-field">
          <label for="published_date">Published date:</label>
          <input
            type="date"
            name="published_date"
            id="published_date"
            value="<%= new Date(book.published_date).toISOString().slice(0, 10) %>"
            class="form-input"
            disabled
          />
        </div>

        <!-- <input type="hidden" name="title" value="<%= book.title %>" />
        <input type="hidden" name="author" value="<%= book.username %>" />
        <input
          type="hidden"
          name="description"
          value="<%= book.description %>"
        />
        <input
          type="hidden"
          name="published_date"
          value="<%= book.published_date %>"
        /> -->
        <input type="hidden" name="book_id" value="<%= book.id %>" />

        <input type="hidden" name="author_id" value="<%= book.author_id %>" />

        <button class="button-primary button" type="submit">Submit</button>
      </form>
    </main>
  </body>
</html>
